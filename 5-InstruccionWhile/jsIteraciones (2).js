function Mostrar()
{
	var contador=10;

	while(contador>0)
	{
		//alert(contador);	
		document.write("numero "+contador+"<br>");

		contador--;
	}
/* Aca se pidio que el contador llegase a 10 y bajase a 1 por lo que se le dice que el contador
arranca en 10 (var contador=10;) y va decreciendo de a uno al poner al final contador--, tambien
se puede poner contador=contador-n° si se desea bajar de a mas numeros el contador.  */

}//FIN DE LA FUNCIÓN